#
# This module is provided as expat_USE_FILE by expatConfig.cmake.  It can
# be INCLUDED in a project to load the needed compiler and linker
# settings to use expat.
#

IF(NOT expat_USE_FILE_INCLUDED)
  SET(expat_USE_FILE_INCLUDED 1)
  
  # Add include directories needed to use expat.
  INCLUDE_DIRECTORIES(${expat_INCLUDE_DIRS})
  
ENDIF()
